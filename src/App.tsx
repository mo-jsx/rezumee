import { Form, Display } from "./views";

import "./app.scss";

const App = () => {
    return (
        <main className="app">
            <Form />
            <Display />
        </main>
    );
};

export default App;
