import { Form, View } from "./layouts";

import "./app.css";

const App = () => {
    return (
        <main className="app">
            <Form />
            <View />
        </main>
    );
};

export default App;
