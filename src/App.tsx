import { Form, Display } from "./views";

import "./app.css";

const App = () => {
    return (
        <main className="app">
            <Form />
            <Display />
        </main>
    );
};

export default App;
