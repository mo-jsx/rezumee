import { Details, Header } from "../../layouts";

import "./form.scss";

const Form = () => {
    return (
        <div className="form">
            <Header />
            <Details />
        </div>
    );
};

export default Form;
