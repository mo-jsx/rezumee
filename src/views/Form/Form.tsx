import { Details, Experience, Header } from "../../layouts";

import "./form.scss";

const Form = () => {
    return (
        <div className="form">
            <Header />
            <Details />
            <Experience />
        </div>
    );
};

export default Form;
